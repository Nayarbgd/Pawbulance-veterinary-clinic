The desktop hero looks correct. DO NOT MODIFY the desktop version.

The issue only exists on mobile devices.

========================
MOBILE HERO FIX
========================

The current implementation crops the image incorrectly and shows only part of the veterinarian's face.

I want the mobile hero to look visually similar to the desktop composition.

Requirements:

• Keep the same hero image.
• Keep the same text.
• Keep the same CTA buttons.
• Keep the same spacing and branding.

ONLY change how the image behaves on mobile.

========================
IMAGE POSITIONING
========================

For screens below 768px:

Do NOT use default center cropping.

Instead:

background-position: 70% center;

or if using an absolutely positioned image:

object-position: 70% center;

This shifts the image slightly to the right so the woman, dog and veterinarian remain visible.

========================
IMAGE SCALE
========================

Desktop:
object-fit: cover;

Mobile:
scale image down slightly.

Use approximately:

background-size: 160% auto;

or:

transform: scale(0.82);

depending on implementation.

The image should not appear extremely zoomed.

The user must see:

✓ veterinarian
✓ woman
✓ dog

at the same time.

========================
TEXT OVERLAY
========================

The hero text must remain ON TOP OF THE IMAGE.

Do NOT place the image above the text.

Current mobile structure:

IMAGE
TEXT

is incorrect.

Desired structure:

HERO IMAGE BACKGROUND
    └── badge
    └── headline
    └── paragraph
    └── buttons

Everything should be layered on top of the image exactly like desktop.

========================
MOBILE LAYOUT
========================

Hero:

position: relative;
min-height: 85vh;
overflow: hidden;

Background image:

position: absolute;
inset: 0;
width: 100%;
height: 100%;
object-fit: cover;
object-position: 70% center;
z-index: 0;

Content:

position: relative;
z-index: 2;

Apply a subtle white overlay:

linear-gradient(
90deg,
rgba(255,255,255,0.65),
rgba(255,255,255,0.20)
);

The final mobile result should feel like:

• Apple Healthcare landing pages
• Modern veterinary websites
• One single hero composition
• The image remains centered and emotional
• Woman + dog + veterinarian all visible together
• Text floating on top of the image

DO NOT stack the image above the text.
DO NOT crop the image to only show the veterinarian's face.
DO NOT create a separate image container.
Desktop must remain untouched.